{
  "name": "@synarc/agent-sdk",
  "version": "0.1.0",
  "description": "SDK for integrating SynArc governance and treasury on Arc Network",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest run"
  },
  "keywords": ["synarc", "dao", "governance", "arc", "usdc", "web3"],
  "author": "kellycryptos",
  "license": "MIT",
  "peerDependencies": {
    "viem": ">=2.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "viem": "^2.49.3"
  }
}
